# 裸机部署指南（wellflow-saas-backend）

> 适用：**裸机/Linux 服务器**直接部署（不使用 Docker）。部署对象为合并后的双系统仓库：
> `ad/`（广告投放）+ `wellflow/`（电商商拍），顶层 `main.py` 同时启动。

---

## 一、整体流程

```
服务器准备 → 代码上服务器 → 建 .env → 装依赖 → PG 建库迁移 → token_store.db → systemd 启动 → 验证
```

---

## 二、服务器前置准备

```bash
# Ubuntu/Debian
sudo apt update && sudo apt install -y python3.12 python3.12-venv git postgresql

# CentOS/Rocky
sudo dnf install -y python3.12 python3.12-pip git postgresql-server

# 启动 PostgreSQL（如未启动）
sudo systemctl enable --now postgresql
# 创建 wellflow 库（docker-compose 版会自动建；裸机需手动）
sudo -u postgres createdb wellflow
```

---

## 三、代码上服务器

```bash
git clone git@github.com:chenxuwen2022/ad_system.git /opt/wellflow-saas-backend
cd /opt/wellflow-saas-backend
```

或直接从云效拉取（国内服务器更快）：

```bash
git clone sy888@sy888-cn-beijing.devops.aliyuncs.com:codeup/wellflow-saas-backend.git /opt/wellflow-saas-backend
```

---

## 四、配置 .env

```bash
cp .env.example .env
vim .env   # 必填项：
```

| 变量 | 说明 |
|---|---|
| `POSTGRES_PASSWORD` | PostgreSQL 密码（与建库时一致） |
| `DATABASE_URL` | 例：`postgresql+psycopg2://postgres:密码@localhost:5432/wellflow` |
| `DATABASE_URL_ASYNC` | 例：`postgresql+asyncpg://postgres:密码@localhost:5432/wellflow` |
| `QIANCHUAN_SECRET` | 巨量千川应用密钥（`ad/config.py` 读取） |
| `DEEPSEEK_API_KEY` | DeepSeek 素材分析密钥 |
| `NEWAPI_API_KEY` | 中转网关密钥（如使用） |

> `.env` 同时被 `ad/config.py`（读 ad/.env 或根 .env）和 `wellflow/app/config.py` 使用，放**项目根目录**一份即可。

---

## 五、一键部署

```bash
cd /opt/wellflow-saas-backend
chmod +x deploy_bare.sh
./deploy_bare.sh
```

脚本自动完成：依赖安装 → wellflow alembic 迁移 → token_store.db 检查 → systemd 服务创建与启动 → 健康检查。

后续更新代码后：

```bash
git pull && ./deploy_bare.sh --skip-deps --skip-migrate   # 或 --restart 最快
```

---

## 六、token_store.db 处理（重点）

**它是什么**：广告系统的授权凭据库（SQLite），存 `access_token` / `refresh_token` / 有效期。
**关键性质**：已被 `.gitignore` 忽略（`*.db`），**不会随 git 同步到服务器**，必须单独处理。

### 方式 A：复制本机授权（推荐，免重新授权）

```bash
# 1) 本机（Windows 开发机）先停掉服务（PyCharm 停止 / 8010 停止）
# 2) 本机执行：
scp C:\Users\33082\PycharmProjects\wellflow-saas-backend\ad\token_store.db \
    用户@服务器IP:/opt/wellflow-saas-backend/ad/token_store.db
# 3) 服务器重启服务
sudo systemctl restart wellflow-saas
```

- 适用前提：服务器 `.env` 的 `APP_ID` / `QIANCHUAN_SECRET` 与开发机一致（同一个抖音开放平台应用）
- token 过期后系统会用 refresh_token **自动续期**，无需人工
- refresh_token 也有有效期（抖音平台约 30 天，视应用配置），过期后需重新授权

### 方式 B：服务器重新授权（无凭据迁移）

```bash
# 1) 正常启动服务（无 token_store.db 会自动建空表）
# 2) 用 auth_code 授权（和开发机首次授权一样）：
curl -X POST http://服务器IP:8000/api/token/fetch \
     -H "Content-Type: application/json" \
     -d '{"auth_code":"你的auth_code"}'
```

### 备份建议

```bash
# 定期备份 token 库（防丢失）
cp ad/token_store.db ad/token_store.db.bak.$(date +%Y%m%d)
```

---

## 七、systemd 服务说明

脚本生成的 `/etc/systemd/system/wellflow-saas.service`：

```
[Service]
WorkingDirectory=/opt/wellflow-saas-backend
Environment=PYTHONPATH=/opt/wellflow-saas-backend
ExecStart=/opt/wellflow-saas-backend/.venv/bin/python -m uvicorn main:app --host 0.0.0.0 --port 8000 --workers 1
Restart=always
```

**注意**：`--workers 1` 是硬要求——SQLite（token_store.db / 素材标记）不支持多进程并发写，多 worker 会报 `database is locked`。

常用运维命令：

```bash
sudo systemctl status wellflow-saas   # 状态
sudo journalctl -u wellflow-saas -f   # 实时日志
sudo systemctl restart wellflow-saas  # 重启
```

---

## 八、常见问题

| 问题 | 处理 |
|---|---|
| `connection timeout expired`（PG） | 确认 PostgreSQL 已启动、`.env` 的 DATABASE_URL 主机/密码正确、防火墙放行 5432 |
| `relation "task" does not exist` | 说明 wellflow 迁移没跑：`cd wellflow && PYTHONPATH=.. .venv/bin/alembic upgrade head` |
| 端口被占 | 改 `PORT` 环境变量或在服务文件里改端口 |
| 外网访问不了 | 云服务器安全组/防火墙放行 8000 端口 |
| `database is locked` | uvicorn 启用了多 worker，改为 `--workers 1` |
| 素材上传后页面不显示 | 检查 `ad/media_storage/`、`ad/upload_materials/` 目录写权限（运行用户需可写） |
| 健康检查 wellflow 为降级 | 看日志里 `[商拍子系统]` 初始化信息，多为 PG 未迁移或未建库 |

---

## 九、验证清单

```bash
curl http://127.0.0.1:8000/health
# 期望：{"status":"ok","ad_system":"ok","wellflow":"ok","langgraph_available":true,...}

curl "http://127.0.0.1:8000/api/launch_records?limit=5"   # 投放记录表（PG）
curl "http://127.0.0.1:8000/api/products?advertiser_id=1854728640974923"  # 广告商品
```

- 浏览器打开 `http://服务器IP:8000/` → 广告投放台
- 浏览器打开 `http://服务器IP:8000/docs` → API 文档
