# 穿搭库数据库化实施改动说明

> 变更:migrate-outfit-to-db(OpenSpec 提案 /Users/lhz/openspec/changes/migrate-outfit-to-db/)
> 实施日期:2026-09-16 | 规范参照:同事模特库(mannequin)四层结构

## 一、新建文件(9 个)

| # | 文件 | 说明 |
|---|---|---|
| 1 | `wellflow/app/models/outfit_models.py` | outfit 表模型(items/dims JSONB,仿 mannequin_models) |
| 2 | `wellflow/app/repositories/outfit_repo.py` | 数据访问层:增删改查 + scope/q/dims 筛选分页 + WF-O 编号分配 |
| 3 | `wellflow/app/schemas/outfit_schemas.py` | Pydantic 请求/响应模型(JSON body,独立文件不碰同事 asset_schemas) |
| 4 | `wellflow/app/api/outfit.py` | 全部接口(658 行):CRUD(入库/列表/详情/编辑/删除,官方 403)+ 交互端点(ai-extract/ai-status/flatlay)+ 新增 auto-tag 打标 |
| 5 | `wellflow/migration/versions/e7f8a9b0c1d2_add_outfit_table.py` | 建表迁移(挂在同事 b2c3d4e5f6a7 之后,保持单链) |
| 6 | `wellflow/scripts/import_outfits_to_db.py` | 一次性导入:library.json 8 条 outfit 记录 → outfit 表(幂等) |
| 7 | `.env` | 本地配置:DATABASE_URL / DATABASE_URL_ASYNC / NEWAPI_API_KEY(gitignore 保护,不入库) |
| 8 | `穿搭库数据库化改动说明.md` | 本文件 |

## 二、从 master 取用的同事文件(原样取用,未修改)

| 文件 | 说明 |
|---|---|
| `wellflow/app/api/uploads.py` | 统一图片上传接口(同事写的,我们分支原本没有) |
| `wellflow/app/api/utils.py` | 取 master 版(uploads 依赖的 StandardResponse;原 ok/fail/page 签名一致,安全超集) |
| `wellflow/app/utils/image_store.py` | 取 master 版(含 save_asset_upload) |
| `wellflow/migration/versions/b2c3d4e5f6a7_add_mannequin_log_round_cols.py` | 同事的迁移(保持迁移链与 master 一致) |

## 三、修改的现有文件(2 个,均为最小改动)

1. **`main.py`**:旧穿搭挂载(3 行)替换为新挂载(2 行)—— `wellflow/app/api/outfit.py` 与 `uploads.py`,prefix="/api"
2. **`wellflow/app/config.py`**:仅追加 2 个字段(`llm_model_node3 = google/gemini-3.7-flash`、`llm_model_text = qwen/qwen-turbo`),与 master 一致,其他行未动

## 四、删除的文件(2 个,均是我之前写的)

- `routes/outfit_routes.py`(旧 JSON 版穿搭 API)
- `routes/outfit_ai.py`(旧 AI 拆解/平铺)

## 五、接口对照(前端需知)

| 变化 | 旧 | 新 |
|---|---|---|
| 列表 | GET /api/assets?category=outfit | **GET /api/outfit**(读库,筛选参数同义) |
| 详情 | GET /api/assets/{编号} | **GET /api/outfit/{数字id}** |
| 编辑/删除 | /api/outfit/{编号}(FormData) | **/api/outfit/{数字id}**(JSON body) |
| 入库 | POST /api/outfit(FormData) | **POST /api/outfit(JSON body)** |
| 上传 | 直接传文件给 ai-extract | **POST /api/wellflow/image/uploads?session_id=xxx** |
| 拆解 | ai-extract 收文件 | ai-extract 收 **{original_uri, session_id, mode}** JSON |
| 平铺 | flatlay 收 items_json 表单 | flatlay 收 **{items:[storage_uri], session_id}** JSON |
| **打标(新)** | 无 | **POST /api/outfit/auto-tag {image_uri}** → 六组维度+描述 |
| 图片产物 | /static/outfit_ai/ | **/uploads/{session_id}/outputs/** |
| 响应包装 | {code:0,data} | **{code:0,data,message:"ok"}**(StandardResponse) |

## 六、你需要在自己终端执行(venv 环境)

```bash
# 1. 建表(所有 alembic 迁移)
python -m alembic upgrade head        # 在项目根目录,用 venv 的 python

# 2. 导入 8 条旧记录
cd wellflow && python scripts/import_outfits_to_db.py

# 3. 重启服务
python main.py
```

验证:
```bash
curl http://localhost:8000/api/outfit          # 应返回 8 条导入记录
curl http://localhost:8000/health              # checkpointer_available 应为 true
```

## 七、按你指示未做的事

- **两个测试页面(穿搭库.html / 创建穿搭.html)未更新**(你要求链接直接发送,不写入代码)。注意:旧页面调的是旧接口,数据库版上线后旧页面会失联;需要时我再更新
- 未运行 alembic/导入(本机沙箱无法执行 venv 命令,留给你终端执行)
- 未提交 git(由你自己提交)
