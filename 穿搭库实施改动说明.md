# 穿搭库实施改动说明

> 变更:add-outfit-library(OpenSpec 提案 /Users/lhz/openspec/changes/add-outfit-library/)
> 实施日期:2026-09-16

## 一、新增文件(9 个)

| # | 文件 | 说明 |
|---|---|---|
| 1 | `scripts/outfit_seed_data.py` | 8 套官方穿搭种子数据常量表(名称/描述/单品/六组维度,数据取自 PM demo) |
| 2 | `routes/outfit_routes.py` | 穿搭库 API:详情/维度枚举/创建/编辑/删除/上传,含线程锁与官方资产 403 保护 |
| 3 | `routes/outfit_ai.py` | 演示模式 AI 拆解(异步任务+轮询,按需生成+缓存)+ PIL 平铺总图合成 |
| 4 | `static/穿搭库.html` | 穿搭库首页:范围筛选/搜索/六组维度筛选/卡片/详情相册(lightbox)/编辑/删除 |
| 5 | `static/创建穿搭.html` | 三步创建流程:图片来源三选一 → AI 拆解轮询+单品勾选 → 平铺合成+自动名称/标签/描述+入库 |
| 6 | `static/assets/outfits/`(目录,30 张) | 官方种子图片(wf-o004~wf-o011 及单品图),复制自 demo 前端资产 |
| 7 | `static/assets/outfit-demo/`(目录,2 张) | 演示模式示例素材(original.png 示例照片 + flatlay-layered.png 参考) |
| 8 | `assets_data/library.json.bak_20260916_081451` | 迁移前数据备份(回滚用) |
| 9 | `穿搭库实施改动说明.md` | 本文件 |

## 二、修改的现有文件(2 个,均为最小改动)

### 1. `main.py`(新增 4 行:2 行 import + 2 行挂载)
```python
# import 区新增:
from routes.outfit_routes import assets_router as outfit_assets_router, outfit_router
from routes.outfit_ai import router as outfit_ai_router

# 路由挂载区新增(在 asset_router 之后):
app.include_router(outfit_assets_router)  # 穿搭库:GET /api/assets/{id} 详情
app.include_router(outfit_router)        # 穿搭库:/api/outfit 写端点 + dimensions
app.include_router(outfit_ai_router)     # 穿搭库:/api/outfit ai-extract/ai-status/flatlay
```

### 2. `.gitignore`(追加 2 行)
```
# AI 穿搭拆解/平铺图运行时产物
static/outfit_ai/
```

## 三、数据变更

- `assets_data/library.json`:删除旧 8 条无图 outfit 记录(WF-O001~O008 旧维度口径),
  写入 8 条官方种子记录(WF-O004~WF-O011,新六组维度口径、含图片与单品清单),
  其余 51 条非 outfit 记录逐条比对**完全一致**。
- 备份:`assets_data/library.json.bak_20260916_081451`,回滚时覆盖回 library.json 即可。

## 四、新增 API 一览

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/assets/{asset_id}` | 单条资产详情(全分类通用,此前缺失) |
| GET | `/api/outfit/dimensions` | 六组维度枚举,颜色分「基础色/彩色」 |
| POST | `/api/outfit` | 创建穿搭(original_url + cover_url + items_json) |
| PUT | `/api/outfit/{id}` | 编辑穿搭(官方资产 403) |
| DELETE | `/api/outfit/{id}` | 删除穿搭+关联图片文件(官方资产 403) |
| POST | `/api/outfit/upload` | 通用图片上传(编辑换图用) |
| POST | `/api/outfit/ai-extract` | 演示模式拆解(异步,返回 task_id;支持上传/图片直链/示例照片) |
| GET | `/api/outfit/ai-status?task_id=` | 轮询拆解状态(processing/done/failed) |
| POST | `/api/outfit/flatlay` | 已选单品 → PIL 合成 4:3 平铺总图(1448x1086) |

## 五、与设计方案的偏差(均经你确认)

1. **零改动方案**:`routes/asset_routes.py` 未做任何修改。线程锁、详情端点均落在新文件
   `routes/outfit_routes.py`(同前缀路由实现)。代价:旧资产接口的并发锁不补(维持原状)。
2. **迁移不落脚本**:按你的选择,迁移以一次性命令执行(结果已写入 library.json),未创建迁移脚本文件。
3. **演示单品图最终方案**:原计划的「AI 预生成 → flatlay 裁剪兜底」两条路走不通后,
   改为「按需生成+磁盘缓存」;随后经本地代理从线上 demo 站(wellflow-studio)成功获取
   6 张演示单品图(1254x1254 PNG)落 `static/assets/outfit-demo/` 随代码提交。
   当前演示模式完全离线可用:缓存命中直接返回,无需 AI 网关;
   图片缺失时仍会尝试按需 AI 生成(降级链 mai-image-2.5 → gpt-image-2.5-flare → gpt-image-2)。

## 六、验证情况

### 已完成(本机)
- 全部 Python 文件语法检查通过;两个页面的 JS 语法检查通过(node --check)
- 逻辑层测试(桩 fastapi 直接调用端点函数):**21/21 通过**(outfit_routes)+ **13/13 通过**(outfit_ai)
  - 覆盖:维度枚举、详情/404、创建(自动编号 WF-O012、图片落盘、tags/desc 自动生成)、
    官方资产 403(编辑/删除)、编辑、删除+图片清理、并发 10 条无丢失、编号无重复、
    ai-extract 任务生命周期与错误路径、flatlay 参数校验与布局规则
- 数据迁移校验:outfit 恰好 8 条、图片全部存在、其余分类逐条一致
- 测试均用临时目录隔离,**未污染真实数据**

### 待你环境验证(本机无法启动服务:venv 被安全策略隔离,系统 python 无 fastapi/PIL)
1. 启动服务(你平时的方式,venv + uvicorn 或 docker)后 curl `/api/outfit/dimensions`、
   `/api/assets/WF-O004` 确认返回正常
2. 浏览器打开 `http://localhost:8000/static/穿搭库.html` 与 `创建穿搭.html` 走全流程
3. 演示模式首次拆解需 AI 网关可达(192.168.110.254),完成后 `static/assets/outfit-demo/`
   会生成 6 张单品图;网关不可达时任务返回 failed 并给出明确提示
4. flatlay 合成依赖 Pillow(venv 已装),传 1~6 件单品各测一次

## 七、回滚方式

- 数据:`assets_data/library.json.bak_20260916_081451` 覆盖回 `assets_data/library.json`
- 代码:`git checkout` 或 `git revert` 本次提交;`main.py`/`.gitignore` 改动仅 6 行
- 新增文件直接删除即可,不影响旧系统
